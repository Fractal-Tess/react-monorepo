{
  "name": "@workspace/convex",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "infisical run --path=/convex -- convex dev --local",
    "dashboard": "infisical run --path=/convex -- convex dashboard",
    "logs": "infisical run --path=/convex -- convex logs",
    "lint": "bunx ultracite check .",
    "format": "bunx ultracite fix .",
    "seed": "infisical run --path=/convex -- convex run init:seed '{}'",
    "test": "infisical run --path=/convex -- bun test",
    "typecheck": "infisical run --path=/convex -- tsc --noEmit && infisical run --path=/convex -- tsc -p convex"
  },
  "exports": {
    "./api": "./convex/_generated/api.js"
  },
  "dependencies": {
    "@workspace/shared": "workspace:*",
    "convex": "^1.35.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2"
  }
}
