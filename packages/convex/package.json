{
  "name": "@workspace/convex",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "infisical run -- convex dev --local",
    "predev": "infisical run -- convex dev --until-success && infisical run -- convex dashboard",
    "logs": "infisical run -- convex logs",
    "lint": "bunx ultracite check .",
    "format": "bunx ultracite fix .",
    "test": "infisical run -- bun test",
    "typecheck": "infisical run -- tsc --noEmit && infisical run -- tsc -p convex"
  },
  "exports": {
    "./api": "./api.ts",
    "./messages": "./messages.ts"
  },
  "dependencies": {
    "@workspace/shared": "workspace:*",
    "convex": "^1.35.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2"
  }
}
