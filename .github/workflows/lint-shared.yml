name: Lint Shared

on:
  pull_request:
    paths:
      - ".github/workflows/lint-shared.yml"
      - "biome.jsonc"
      - "bun.lock"
      - "package.json"
      - "packages/shared/**"
  push:
    paths:
      - ".github/workflows/lint-shared.yml"
      - "biome.jsonc"
      - "bun.lock"
      - "package.json"
      - "packages/shared/**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run --cwd packages/shared lint
