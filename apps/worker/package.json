{
  "name": "@workspace/worker",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "infisical run --path=/worker -- bun --watch index.ts",
    "lint": "bunx ultracite check .",
    "start": "infisical run --path=/worker -- bun run index.ts",
    "test": "infisical run --path=/worker -- bun test",
    "typecheck": "infisical run --path=/worker -- tsc --noEmit"
  },
  "dependencies": {
    "@workspace/convex": "workspace:*",
    "@workspace/pocketbase": "workspace:*",
    "@workspace/shared": "workspace:*",
    "convex": "^1.39.1",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "@workspace/typescript-config": "workspace:*",
    "typescript": "^6.0.3"
  },
  "module": "index.ts"
}
